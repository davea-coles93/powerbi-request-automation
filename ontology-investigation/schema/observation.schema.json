{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "observation.schema.json",
  "title": "Observation",
  "description": "Raw data that is captured at a point of activity - where data is born",
  "type": "object",
  "required": ["id", "name", "entity_id", "system_id"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier"
    },
    "name": {
      "type": "string",
      "description": "Display name"
    },
    "description": {
      "type": "string",
      "description": "What this observation captures"
    },
    "entity_id": {
      "type": "string",
      "description": "Reference to the entity this observation describes"
    },
    "system_id": {
      "type": "string",
      "description": "Reference to the system where this observation is captured"
    },
    "source_actor": {
      "type": "string",
      "description": "Who or what creates this observation (role or system component)"
    },
    "reliability": {
      "type": "string",
      "enum": ["High", "Medium", "Low"],
      "description": "Data reliability level (can override system default)"
    },
    "volatility": {
      "type": "string",
      "enum": ["Point-in-time", "Accumulating", "Continuous"],
      "description": "How this observation changes over time"
    },
    "notes": {
      "type": "string",
      "description": "Additional notes"
    }
  }
}
